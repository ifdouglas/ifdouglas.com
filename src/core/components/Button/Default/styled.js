import styled from 'styled-components'

const Button = styled.a`
  text-decoration: none;
  cursor: pointer;
  transition: 0.4s;

  :hover {
    transform: scale(1.1)
  }
`

export default {
  Button,
}
